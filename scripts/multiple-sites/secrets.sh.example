#!/bin/bash
# Copy this file to secrets.sh and fill in your values.
# secrets.sh is excluded from git — never commit real values.

# The Pantheon organization UUID (found in the Org Dashboard URL or terminus org:list)
export PANTHEON_ORG="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
